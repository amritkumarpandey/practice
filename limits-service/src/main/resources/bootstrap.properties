#In order to read the configuration from the config server,
# the properties file must be renamed to bootstrap.properties

spring.application.name=limits-service

spring.cloud.config.uri= http://localhost:8888

#configuration to choose profile
spring.profiles.active=dev
